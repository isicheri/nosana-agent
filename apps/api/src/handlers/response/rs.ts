// if (userMessage.includes("summarize")) {
//     call("/api/summarize", { content, style, sessionId });
//   } else if (userMessage.includes("flashcard") || userMessage.includes("quiz")) {
//     call("/api/flashcard", { content, style, sessionId });
//   } else {
//     call("/api/chat", { message, sessionId });
//   }
  